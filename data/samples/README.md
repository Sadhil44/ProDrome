# data/samples/

Small committed fixtures so everyone can run everything without waiting on another workstream's output — see [SETUP.md](../../SETUP.md) §3 and §8.

This directory is the one exception to the `data/*` gitignore rule. Nothing has been committed here yet; the first fixture should be a small `metrics.parquet` matching the schema in SETUP.md §7.
